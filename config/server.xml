<server description="default servlet engine">
    <!-- 2 -->
    <featureManager>
        <feature>servlet-3.0</feature>
        <feature>jndi-1.0</feature>
        <feature>adminCenter-1.0</feature>
    </featureManager>

    <httpEndpoint host="*" httpPort="9080" httpsPort="9443" id="defaultHttpEndpoint"/>

    <keyStore id="defaultKeyStore" password="Liberty"/>
    
    <!-- Define a keystore for the HTTPS port -->
    <basicRegistry id="basic">
        <user name="admin" password="admin"/>
        <user name="nonadmin" password="nonadmin"/>
    </basicRegistry>
    
    <!-- Assign 'admin' to Administrator -->
    <administrator-role>
        <user>admin</user>
    </administrator-role>
    
    <remoteFileAccess>
        <writeDir>${server.config.dir}</writeDir>
    </remoteFileAccess>

    <jndiEntry jndiName="backbase/auditing-config" value="/config/dropins/auditing.properties"/>

    <jndiEntry jndiName="java:comp/env/backbase/gateway-config" value="/config/dropins/gateway.properties"/>
</server>